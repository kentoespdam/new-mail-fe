### **Issue #1.3: TanStack Query & Table Global Setup**

**Epic**: EPIC 1 - Init & Infrastructure
**Parent Issue:** [Issue #2](../0-blueprint/1-init-infrastructure.md)

**Description:**
Konfigurasi *state management* untuk data fetching dan manajemen tabel yang kompleks menggunakan TanStack suite.

**Tasks:**
- [ ] Setup `QueryClientProvider` di `src/app/layout.tsx`.
- [ ] Konfigurasi `defaultOptions` pada QueryClient (e.g., `retry: false`, `refetchOnWindowFocus: false`).
- [ ] Install **TanStack DevTools** dan aktifkan hanya pada mode *development*.
- [ ] Buat komponen wrapper `DataTable` dasar menggunakan TanStack Table v8 yang mendukung *server-side pagination*.

**Acceptance Criteria:**
- TanStack Query dapat melakukan fetching data dasar.
- DevTools muncul saat aplikasi dijalankan di lokal.
